❤️ 觉得好用？支持一下
若 持仓实时盈亏看板 曾为你提供价值，不妨为它注入动力，助其持续进化

金额随意，1元也是对开源的鼓励。欢迎在赞赏时备注留言 (´▽`ʃ♡ƪ)
<img width="245" height="245" alt="donate" src="https://github.com/user-attachments/assets/d1286738-681e-4dc3-8713-bce8b3b2cbec" />



# 持仓实时盈亏看板（浏览器扩展版）

本地持仓盈亏 / 基金估值 / 价格异动 + 新公告 **桌面提醒**的 Chrome / Edge 扩展（MV3）。

所有数据只存在你自己电脑的浏览器本地存储里，**不上传任何服务器**。

## 功能特性

- 基金、股票实时盈亏与估值展示
- 涨跌幅异动提醒、基金 / 股票新公告提醒
- 桌面系统通知（扩展原生 `chrome.notifications`，比网页通知更稳）
- 单品种自定义阈值、单股票公告独立开关
- 数据全部存本地，零上传、零账号

## 安装（开发者模式加载，免上架商店）

1. 获取本仓库：
   - `git clone <本仓库地址>`，或
   - 点 GitHub 页面 **Code → Download ZIP** 解压
2. 浏览器地址栏打开：
   - Chrome → `chrome://extensions`
   - Edge   → `edge://extensions`
3. 右上角打开 **「开发者模式」**（Developer mode）
4. 点左上角 **「加载已解压的扩展程序」**（Load unpacked），选择本仓库根目录
5. 加载成功后，点工具栏拼图图标 🧩 把「持仓实时盈亏看板」钉到工具栏

## 使用

- 点工具栏图标打开看板 → **添加基金 / 添加股票**（输入代码即可）
- 进入 **提醒设置**：
  - 勾选「桌面通知」，点右侧「点击授权」→ 浏览器弹窗点「允许」
  - 「自动刷新间隔」设成 > 0（例如 `1`，代表每分钟）—— 这步必须有，否则不检查就不会提醒
  - 保存
- 把看板标签页保持打开（可最小化、可切到别的窗口），有异动 / 公告即弹系统通知

## 给朋友分发

- 直接分享本仓库链接，或到 GitHub **Releases** 下载打包好的 zip
- 朋友加载的是独立副本，数据互不干扰，看不到你的持仓

## 注意事项

- 开发者模式加载的扩展**不会自动更新**；升级时重新拉取代码、回扩展页点 🔄 重新加载即可
- 当前通知依赖「看板标签页保持打开」（监控跑在标签页内）；如需彻底关页也能弹，可把监控移入后台 `service worker`
- 行情使用东方财富 / 新浪 / 腾讯接口。

## 隐私

所有持仓、提醒设置仅存于本地浏览器存储（`chrome.storage` / `localStorage`），无任何网络上传行为。

## 更新日志

- **v1.0.7（2026-08-26）**：基金公告提醒精准打开修复——基金分支借用股票全文搜索接口会混入无关股票噪声（如 110022 串到"欣禧股份"半年报），点开详情对不上号；把 `securityShortName` 严格匹配过滤扩展到基金分支，噪声被拒、真基金公告通过，详情接口对基金 artCode 直接返回正文。公告弹窗「打开原文」按钮改用 API 返回的 `attach_url` PDF 原件链接（之前写的 HTML 详情页路径被东财 302 跳到列表页，形同没打开）。manifest 升 1.0.7。另含代码质量优化：图表 resize 监听器内存泄漏修复（模块级 chartInstance 复用）、移除重复的 ECharts 引入、隐私页 CSS 变量对齐（--line→--border）、持仓刷新改为每批 4 只并发（processHolding + runHoldings）；移除每只基金的无条件 `[ext] dump` 调试日志（发布噪声，其余 warn/error/条件日志保留排障）。**修复 v55 漏改的 `chart.setOption` → `chartInstance.setOption(opt, true)`（未定义变量 ReferenceError 导致图表完全无数据）**。**公告弹窗新增「速读卡片」**——原弹窗打开只能直接看 800 字法律套话正文，要从大段套话里翻出"每10股派X元"等核心要点，体感差；改为从标题+正文中提炼「类型标签 + 3-4 条关键数字/事件」渲染到正文之上成为一张浅蓝渐变卡片（如"分红派息 / 每 10 股派 0.50 元（含税）/ 合计派现 2,085 万元 / 基数 4.17 亿股"），完整正文依旧可滚到下方查阅，原文 PDF 链接不变；纯本地规则化（无网络依赖、零失败），覆盖 17 种常见公告类型，未识别类型卡片自动隐藏。**v59 修复翠微股份「董事会决议公告」上漏渲染**：`detectTag` 加标题强信号（`/董事会.{0,6}决议公告|第N届董事会/` 锁定董事会决议）防正文里"聘任总经理"把决议类型挤掉；`stripHtml` 正则收紧不误吃 `<公司章程>` 章节标题括号；决议分支优先抓"审议通过"紧跟的《议案名》区分法律引用 vs 议案；决议类型 items 上限放宽到 6 条（翠微股份这次命中 6 条真议案）。**v60 速读卡片新增 4 类公告支持**：定期报告（半年报/半年报摘要/季报/年报/中期报告/经营数据公告）按"营业收入/利润总额/归母净利润/经营性现金流+同比"通用模板抓，半年报摘要补"总资产/归母净资产较上年末"；股东会通知抓"召开日期/时间/股权登记日/召开地点"；经营范围变更抓"修订条款第N条"+"涉及：经营范围/对外担保"+"尚需股东会审议"；公司章程抓章节数；stripHtml 多处理 `&minus;` / `&mdash;` / `&middot;` 三个常见实体（表格里负号常写为 `&minus;`）。
- **v1.0.6（2026-08-26）**：「我的股票」表列序重排（市值→现价后、涨跌幅→成本价后）；批量编辑保存丢失「类型」字段修复（存量「其他」恢复）；公告类提醒可点开查看详情（扩展内弹窗拉东财正文 + 打开原文兜底）；修复 v49 弹窗误调未定义函数导致点不了；浅色主题识别卡片字体对比提升；隐私政策页跟随浅/深主题（外部 privacy.js 兼容 MV3 CSP）；manifest 升 1.0.6。
- 完整历史见 [CHANGELOG.md](./CHANGELOG.md)。
