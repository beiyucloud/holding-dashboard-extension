# 更新日志

## v1.1.5（2026-09-23）
### 新增
- **右下角新增「好评浮窗」（简版）**：页面右下角常驻一张小卡「喜欢这个看板？/ 去商店给个好评 ⭐ →」，点击在新标签打开本扩展的 Edge 加载项商店详情页。点小卡右上角「×」后 30 天内不再出现，点过链接后 180 天内不再出现——都只是暂停提示、不是永久关闭（误点也还有回头路）。桌面小组件（`?widget`）与还没有任何基金 / 股票的空白看板都不弹；等首屏数据刷新完约 5 秒才出现，不打断打开看板的第一眼。位置为固定右下角（z-index 60），不遮挡弹窗（弹窗遮罩层级更高）。
- **「☕ 打赏」弹窗新增好评引导**：赞赏码与渠道文字下方新增一行「也欢迎到商店给个好评 ⭐ →」，点击在新标签打开本扩展的 Edge 加载项商店详情页，方便顺手留下评价（评分与评价数量是商店推荐权重的重要来源）。放在打赏弹窗而非顶栏，是因为这里本身就是「支持作者」的语境、转化更自然，也不会挤占顶栏空间。写法与其他外链入口一致（`<a target="_blank" rel="noopener noreferrer">` 直跳，无需 JS、不受扩展 CSP `connect-src` 限制），桌面小组件模式下随打赏按钮一并不可达。

- **点击名字看详情（股票 / 场内基金 / 场外基金 / 指数）**：持仓表里的名字、指数快照卡的名字都可点开。个股弹窗顶部是名称·代码·现价·涨跌额与涨跌幅·最后更新时间，下面 7 个周期标签（分时 / 5日 / 日K / 周K / 月K / 季K / 年K），图表下方 12 项盘口数据（昨收·今开·最高·最低·均价·振幅·成交量·成交额·量比·换手率·流通市值·总市值）与**五档买卖盘**（买卖各五档的价格与挂单量，按昨收红绿着色，无挂单数据的标的自动隐藏）。分时图带**分时量柱**（逐分钟量，涨红跌绿），价格线 + 均价线（**指数没有均价线**，见下方修复项），右轴按昨收映射成涨跌幅、昨收虚线做基准；K线为红涨绿跌蜡烛图 + 成交量副图，默认显示最近 60 根、可缩放平移。腾讯只提供日/周/月三档 K 线，**季K / 年K 由月K本地聚合**（开盘取组内首月、收盘取末月、高低取极值、量求和），可回溯约 14 年；并记住上次看过的周期（首次为分时）。数据通道：行情快照走腾讯 `qt.gtimg.cn`（页面直连失败自动落 SW 中转），分时 / K 线走腾讯 `web.ifzq.gtimg.cn`（**页面直连优先、SW 中转兜底**，该域名已加入 manifest 的 `connect-src` 白名单）。弹窗只展示公开行情数据，不含持仓数量与金额，因此不参与隐私打码。
- **场外基金新增「净值走势」详情**：在「我的基金」表里点基金名字，弹出净值详情——顶部是最新单位净值与当日涨跌幅、净值日期，下面 6 个区间标签（近1月 / 近3月 / 近6月 / 近1年 / 近3年 / 成立来），图表为单位净值走势（区间起点画虚线做基准），下方 12 格数据（最新净值·日涨跌·累计净值·成立来·近1月·近3月·近6月·近1年·近3年·区间涨幅·区间最高·区间最低）。数据来自天天基金公开净值文件（单位净值与累计净值全历史），区间与阶段涨幅都按净值序列本地计算。
- **详情弹窗底部新增「加仓 / 减仓」入口**：只有该标的确实在本看板的持仓里时才出现，点了直接进入既有的加仓 / 减仓弹窗（复用原流程，不新增任何写入口）。
- **场外基金详情新增「持仓明细（重仓股）」与「基金概况」**：点基金名字打开的弹窗顶部多了三个主视图——「净值走势 / 持仓明细 / 基金概况」，选择会被记住。「持仓明细」列出该基金最近一个季度的前十大重仓股（股票名称与代码 / 当前价格 / 当日涨跌幅 / 占净值比例 / 较上期，并标出季报截止日期与对比的上期日期），「较上期」是**占净值比例相对上一季度的变动**（如凯莱英 本季 10.57% − 上季 7.15% = ↑3.42%），上季未持有的标「新增」；价格与涨跌幅用持仓代码批量查当前行情快照补齐（季报只披露季度末持仓，价格非季报时点价）。「基金概况」列出基金全称 / 简称 / 代码 / 类型 / 发行日期 / 成立日期与规模 / 净资产规模与份额规模 / 管理人 / 托管人 / 基金经理人 / 成立来分红 / 管理托管与销售服务费率 / 认购申购赎回费率 / 跟踪标的 / 业绩比较基准。数据均来自天天基金公开的 F10 基金档案，只做信息整理、不构成投资建议。
- **基金概况里的「基金经理人」可点开详情（基金经理变动一览 + 现任基金经理简介）**：点基金名字 → 「基金概况」→ 点「基金经理人」那一格，进入基金经理详情（弹窗内的二级视图，顶部与底部各有一个「‹ 返回基金概况」，tab 行停在「基金概况」上）。内容三块：① **基金经理变动一览**——每个任职区间一行，列出起始期 / 截止期 / 基金经理（同一区间有多位经理时一并列出）/ 任职期 / 任职涨幅；② **现任基金经理简介**——头像（该经理暂无照片时用姓氏首字占位）、姓名、上任日期、管理年限（按上任日期至今日的自然日折算，同时给天数与约几年）、个人简历全文，同一基金有多位现任经理时逐位展示；③ **历任基金一览**（默认收起，点一行展开）——该经理管理过的其他基金：名称与代码 / 类型 / 任职区间 / 任职天数 / 任职回报 / 同类排名。「任职期」按天天基金口径折算为天数（`8年又279天` = 365×8 + 279 = 3199 天），与天天基金 App 显示一致。数据来自天天基金公开的 F10 基金经理页（`fundf10.eastmoney.com/jjjl_{code}.html`，该页无需 Referer），只做公开信息整理、不构成投资建议；历史任职回报不代表未来业绩。
- **K 线图上标注「区间最高 / 区间最低」**：日K / 周K / 月K / 季K / 年K 都会在当前**可见区间**内自动标出最高点与最低点的数值——最高点上方标红、最低点下方标绿，随缩放与平移实时重算，看到哪一段标的就是哪一段的极值（默认窗口是最新 60 根）。y 轴上下各留 14% 空间，保证数值气泡不被画布边缘裁掉。气泡里**只显示数值、不写「最高 / 最低」二字**，方向靠颜色（红 = 最高 / 绿 = 最低）与上下位置区分。口径与盘口 12 格里的「最高 / 最低」不同（那是当日快照口径）。
### 修复
- **修复「北证50 / 标普500 / 道琼斯 / 纳斯达克 点开详情没有 K 线图，美股分时也是空白」（2026-09-23，v176）**：现象是这四个指数（以及所有美股标的）的详情弹窗里分时与 K 线都显示「暂无走势数据」，而卡片上的报价数字一直是好的——三个互不相同、但**都是静默失败**的根因：① **K 线控制器选错了代**。腾讯有两代 K 线接口，旧代 `fqkline` 对北证50 与美股指数只肯回 **1 根**（当日那根），被代码里 `rows.length < 2` 的判空吞掉，整块就变「暂无走势数据」；新代 `newfqkline` 是旧代的**严格超集**——北证50 日K 240+ 根、标普500 日K 321 / 周K 320 / 月K 320（可上溯到 2000 年），而 A 股 / 港股 / 场内基金的返回结构完全一致（个股 `qfqday`、指数与港股 `day`，两种键名解析都已兼容），所以只换路径即可，一处改动同时修好这两类标的。② **分时也要分市场**。美股的当日分时不在 `minute/query`——该端点对美股既不报错也不给历史，只回「当前那一分钟」一个点，同样被 `pts.length < 2` 吃掉；真正给全序列的是**大小写敏感**的 `usMinute/query`（实测四只美股指数与美股个股各 148 点、带昨收）。③ **美股个股的 K 线代码必须带交易所后缀**（`usAAPL` → `usAAPL.OQ`），裸码同样只回 2 根；后缀恰好就是快照的 `[2]` 段，所以实现上只在**裸码取不到数据时**才回查一次快照拿后缀，指数（`usINX` / `usDJI` / `usIXIC` / `usNDX`）裸码直接可用、不会多花这个请求。顺带把美股也没有数据源的「5日」tab 一并隐藏（`day/query` 对 `us*` 恒返回 `param error`），切回 A 股 / 港股自动恢复；美股分时的 x 轴刻度改用「每小时整点」——美股时段是 09:30~16:00（美东），原来那五个 A 股整点标签一个都不命中，而按偶数整点算只剩 3 个、太稀疏。
- **修复「港股 / 美股指数的盘口显示一整排垃圾值」（2026-09-23，v176）**：腾讯的三套快照布局字段位置完全不同（A 股 88 段 / 港股 78 段 / 美股 73 段），而详情弹窗原来一律按 A 股偏移取值，于是——恒指与标普的「量比」实际是 **52 周最低价**（恒指 22518.00、标普 6316.91），「成交额」是把「成分股成交量 × 指数点位」当成钱看（标普 801879842.92 亿），成交量按「手」标注而实际单位是「股」，换手率 / 流通市值 / 总市值则恒为 `--`。现按实测字段重排：**港股 / 美股个股 12 格**（昨收 / 今开 / 最高 / 最低 / 涨跌额 / 振幅 / 成交量 / 成交额 / 流通市值 / 总市值 / 52周高 / 52周低）、**港股 / 美股指数 8 格**（去掉对指数没有意义的成交额与市值），并在格子里写明单位（亿股 / 亿港元 / 亿美元），避免「40109.54亿」被读成人民币。52 周高 / 低在两个布局里**差一位**（A 股在 `[47]/[48]`、港美股在 `[48]/[49]`），已按市场分别取值；A 股与场内基金的 12 格口径**完全未动**。
- **修复「国际金价详情弹窗里报价恒为 `--`、K 线区恒显示暂无走势数据」（2026-09-23，v175）**：指数快照卡上的金价数字一直是好的，只有**点开详情**后坏——顶部报价 `--`、图表「暂无走势数据」、盘口整块「该标的暂无行情快照」。两个互相独立的原因：① **腾讯根本没有外盘 K 线接口**——外盘品种要用 `web.ifzq.gtimg.cn/appstock/app/hfkline/get`，而该控制器已下线（实测返回 `{"code":11,"msg":"Can't load controller:HfklineController"}`），详情弹窗对外盘却仍拼 A 股路径 `fqkline/get?param=hf_GC,…`，接口只回 `{"code":0,"msg":"param error"}`；② **快照解析的布局不匹配**——`qt.gtimg.cn` 对 A 股用 `~` 分隔、对外盘 `hf_` 用**逗号**分隔，原解析器按 `~` 切分后一个字段都取不到，`price` 为 null 就落进了「暂无行情快照」分支。现改为：**外盘（`hf_` 前缀）的 K 线与分时改走新浪国际期货**（`stock2.finance.sina.com.cn/futures/api/jsonp.php/.../GlobalFuturesService.getGlobalFuturesDailyKLine|getGlobalFuturesMinLine`；日K 一次返回全历史，`hf_GC` 约 2589 条 / 10 年，分时给当日逐分钟约 1042 点），**周K / 月K / 季K / 年K 由日K 本地聚合**（聚合新增 week / month 两种分组，自然周按周一起算、跨年不拆周；季度与年度沿用原有口径）；快照另写一套外盘逗号布局解析（`[0]` 现价 `[1]` 涨跌幅 `[4]` 最高 `[5]` 最低 `[6]` 时间 `[7]` 昨收），其中最高 / 最低已与新浪当日日K 的 high / low 交叉验证**完全一致**。外盘确实没有的字段（成交量 / 成交额 / 量比 / 换手率 / 市值）**不再排一排 `--` 占位**——盘口只显示真拿得到的 6 格（昨收 / 最高 / 最低 / 涨跌额 / 振幅 / 时间）并注明「外盘商品：仅提供价格类字段，无成交量 / 市值 / 五档挂单」，五档买卖盘照旧自动隐藏（外盘没有挂单簿）。另外外盘的**交易时段是 06:00~次日 05:00**，分时 x 轴刻度标签相应改为「偶数整点」出标签（原来只认 09:30/10:30/11:30/13:30/14:30 这五个 A 股整点，外盘会得到一条完全没有标签的轴）。「5 日分时」新浪只提供当日、腾讯外盘 K 线又已下线，因此**外盘详情里「5日」tab 直接隐藏**（不给点了必然空白的入口），切回股票 / 指数时自动恢复显示；弹窗标题也保持本地配置标签「国际金价」，不被数据源自己的品种名「纽约黄金」覆盖。取数通道方面：`stock2.finance.sina.com.cn` 没有 `ACAO` 头，需要走 SW 中转，已加入 CSP `connect-src` 白名单；`host_permissions` 里原有的 `*://*.sina.com.cn/*` 已经覆盖该主机，所以**不会要求已安装用户重新授权**。
- **修复「国际金价 / 涨跌家数 / 资金流向图 同时取不到数据」（2026-09-23）**：现象是三块同时变 `--` 或空图，但**不是东财接口坏了**——同一条 `push2 ulist.np` 请求换一条网络路径能拿到完整数据（沪 851/1412/51、深 906/1954/36、京 134/198/14），而本机直连 8/8 全部 `ECONNRESET`；分阶段测出 TCP 握手（89ms）与 TLS 握手（146ms, TLSv1.3）都正常，**发完 HTTP 请求才被 RST**，`push2` / `push2his` / `push2delay` 与 1~88 各镜像主机表现一致，加 `ut` / 加 `cb` / 换 http 明文都无效；同一时刻 `data.eastmoney.com`、`push2ex`、`datacenter-web`、`qt.gtimg.cn`、`web.ifzq.gtimg.cn`、`vip.stock.finance.sina.com.cn`、`d.10jqka.com.cn` 全部 200，浏览器栈（HTTP/2，在真扩展页里 fetch）也照旧失败——所以是**这条线路对 `push2*.eastmoney.com/api/qt/*` 的定向重置**，与 CSP、CORS、代码都无关。三处的处理：① **国际金价**改走腾讯外盘行情 `hf_GC`（COMEX 黄金连续，实测 46ms、带 `ACAO:*`），东财降为备选；顺带修掉一条从 v171 起就存在、但一直被掩盖的解析 bug——原来的腾讯兜底用 `parseTencent`，而它按 A 股字段布局取 `p[3]=现价 / p[4]=昨收`，对 `hf_` 前缀的外盘品种**永远解不出价格**，所以只要东财失败就必然显示 `--`，等于没有兜底。② **涨跌家数**新增同花顺 `d.10jqka.com.cn/v6/realhead/{hs_1A0001|hs_399001}/defer/last.js` 备用源（字段 `38`=上涨 / `39`=下跌 / `37`=市场标的总数，平盘 = 37−38−39；与东财逐市吻合：沪 861/1435、深 915/1972、创业板 571/820），**并修掉了原来那条兜底源的错值**——新浪全 A 列表现在每页只返 100 条且按 `symbol` 升序（北交所打头），算出来是「43 涨 / 54 跌」这种明显错的数，改为「样本不足 2000 只就不采用」（宁可显示 `--` 也不能把 100 只样本当全市场报出去）。③ **大盘资金流向图**原来**根本没有兜底源**，新增新浪分时资金流 `MoneyFlow.ssx_bkzj_fszs?bankuai=hs_a&num=300`（`num` 必传，不传只给最近 60 分钟；返回 241 点且**按时间倒序**，作图前已 reverse），降级时只画「资金净流入」单线并在图上标注来源与口径差异（新浪没有东财那套主力/超大/大/中/小五分类金额）。备用源生效时卡片会改口径标注（如「沪深两市」「新浪（仅资金净流入单线）」），不做数字悄悄变的静默替换。`d.10jqka.com.cn` 带 `Access-Control-Allow-Origin: *`，所以只加进 CSP `connect-src`、**不新增 host_permissions**（避免已安装用户被要求重新授权）。
- **修复「首屏要 30~40 秒才填满」（真扩展实测降到约 4 秒）**：被掐断的 `push2` 系请求每个都要走 3 次重试（`bg.js` 的 0/600/1200ms），一轮刷新要发 5~6 个 → 累计 25 秒以上都耗在等重试，排在后面的「板块资金流」卡在这段时间里看起来像「没数据」（实测 9 秒时整页 canvas 数还是 0）。两处改动：① `bg.js` 对 **push2 系主机只重试 2 次**——保留一次 600ms 重试（Chrome 实例冷启动被风控首拒确实靠它救回），砍掉第 3 次（前两次都在 200ms 内失败时，多等 1200ms 不可能恢复），单接口白等从约 2.2s 降到约 1.0s，也少发一轮注定失败的请求；② 前端新增**失败短路**——某接口出现**网络级**失败（`Failed to fetch` / `net::` / `ECONNRESET` / `ERR_`）后 90 秒内不再重复打它（HTTP 4xx/5xx 不短路，那类重试有意义）。真扩展实测：金价 0.4s、涨跌家数 2.9s、板块资金流 3.3s、资金流向图 4.1s 依次出数（改前 30~40s），同一轮内第二次 `refreshAll` 约 0.5s。
- **修复「东财 5 线资金流一次都没被尝试过」（2026-09-23）**：上一条的短路最初按**主机**做键，粒度太粗。实测同一台 `push2.eastmoney.com` 上各接口命运并不一致——`/api/qt/ulist.np/get` 被 RST，而 `/api/qt/stock/get` 48ms 正常、`/api/qt/stock/fflow/kline/get` 也能成功，**拦截是按接口路径生效的**。于是涨跌家数主源（`ulist.np`）一失败就把 `push2.eastmoney.com` 整台标死，**排在后面的资金流向图连「东财 5 线」都没试**，直接掉到新浪单线（footer 那格停在「…」＝从未发起，就是这么来的）。现短路键改为「主机 + 接口路径」，且金价与资金流两处守卫改用**本接口的 URL**（原来用的是根 URL，永远命不中）；`fund.eastmoney.com/pingzhongdata/{code}.js` 这类「一个接口带不同标的代码」的路径仍按主机短路，避免主机真挂时被逐只基金重试。
- **修复「详情弹窗里点加仓 / 减仓，交易弹窗被压在下面」**：所有弹窗遮罩的基础层级都是 `z-index:99`，谁在上面完全靠 DOM 顺序决定，而详情弹窗写在交易弹窗后面，于是从详情里点「加仓 / 减仓」后交易弹窗出现在详情**之下**，必须先把详情关掉才能操作。现改为显式层级：**详情弹窗 110 < 交易弹窗 120 < 公告弹窗 130**，交易弹窗始终盖在详情之上；关掉交易弹窗即回到原来的详情弹窗（详情不会被顺手关掉）。
- **修复「走势图恒显示暂无走势数据」**：`web.ifzq.gtimg.cn` 没在扩展的 CSP `connect-src` 白名单里，取数被拦，导致行情快照正常而分时 / K 线恒空白；现已放行该域名，并把取数改成「页面直连优先 + SW 中转兜底」。
- **修复指数详情弹窗的分时图与盘口三处口径问题**：① **指数分时不再画均价线**——均价原按「累计成交额 ÷ 累计成交量」算，个股（腾讯给的成交量单位是手，×100 折成股）得到的正是当日均价，但指数的成交量是成分股合计，除出来是「全市场每股均价」（上证指数约 15.7 元），画进分时会把 y 轴从 0 拉到 4000、价格线被压成一条贴底的直线；现在指数（含港股指数）只画价格线 + 分时量柱，均价仍以**快照值**显示在盘口「均价」格里（实测上证 3943.10、沪深300 4530.6，均落在当日最高~最低之间）。② **指数不再显示假五档**——原判定是「5 档位只要解析出数字就显示」，而指数会把 5 档位填 0 或只填一个指数点位、挂单量恒为 0，于是出现一整块「0.00 / 0手」；现改为**价格与挂单量都必须为正**才算真有挂单簿，指数自动隐藏该块，个股不受影响。③ 盘口「均价」若越出当日最高 ~ 最低区间（港股指数按 A 股字段位取会得到负数，如 `-0.27`）显示 `--` 而不是错值。
- **修复「000」开头深市股票（如 000981 山子高科）添加时识别不到 / 认错标的**：同号代码在两个市场可能同时存在——`sh000981` 是上证指数「300分层」，`sz000981` 才是深市股票「山子高科」，原识别流程先探沪市导致指数抢先命中、显示成「股票 300分层」。现在沪深 6 位码先按规范市场（`normStockCode` 的市场归属）优先探测，未命中再回退另一市场；纯指数代码（如 000300 沪深300）仍能正常兜底识别。
- **股票行情通道加 SW 兜底**：腾讯行情（`qt.gtimg.cn`）在扩展页面直连被 CORS / UA / 网络层拦截时，自动落到后台 service worker 中转通道（与基金取数同一条已验证通道）。股票识别与行情刷新不再单点依赖页面直连。
- **修复自动刷新时已隐藏的卡片闪现**：「卡片显示」勾掉的卡片在每次刷新时会被渲染函数按「有无数据」强制显示一帧、再由收尾钩子拉回隐藏，肉眼可见闪烁；现改为渲染时即同时判断「用户开关 AND 有无数据」，刷新全程不再闪现。

> 发布状态：GitHub `main` = `ec512db` 已推送；Edge 加载项商店 2026-09-24 已提交审核（**尚未上架**）。官网仍标注在架 v1.1.4，待商店上架 v1.1.5 后再同步。

## v1.1.4（2026-09-22）
### 优化
- **「设置」与「提醒记录」拆分独立**：顶部新增「设置」按钮（齿轮图标），原「提醒设置」弹窗里的全部配置项（基金/股票公告、提示音、桌面通知、自动刷新、汇率折算、全局阈值、单品种阈值、保存设置）整体迁入独立「设置」弹窗；原「提醒」按钮只保留「提醒记录」列表与「清空」。两弹窗不再互相挤压，配置与记录各归一处。设置弹窗底部新增「指数卡片显示」入口，把指数展示设置一并收拢到「设置」里（无需回到首页点「编辑显示」）。
- **导出 / 导入 / 清空已实现迁入「设置」**：原「批量编辑」弹窗底部的「导出」「导入」「清空已实现」三个数据管理按钮移入「设置」弹窗新增的「数据管理」区，批量编辑弹窗回归纯粹的表格编辑；三个功能的实际行为不变。
- **设置弹窗分类分组**：配置项按功能归入四个带标题的分区——「通知与刷新」（公告 / 提示音 / 桌面通知 / 自动刷新）、「汇率折算」（USD / HKD 手填与实时提示）、「涨跌提醒」（全局阈值 / 单品种阈值）、「数据管理」（导出 / 导入 / 清空已实现），各区块管什么一目了然。
- **单品种阈值默认折叠**：「涨跌提醒」里的单品种阈值列表默认收起，只在折叠头显示摘要——没做过任何单独设置时显示「未设置，全部用全局阈值」，设置过则显示「已设阈值 N 个 · 已关提醒 M 个」；点击折叠头展开完整列表（基金 / 股票 tab）。打开设置弹窗时，只有存在自定义设置才默认展开；保存设置或停 / 恢复提醒后摘要实时刷新。
- **首页卡片可隐藏**：设置弹窗新增「卡片显示」分区，各块内容独立开关，勾掉即整块隐藏不再占位——总览（持仓市值 + 今日盈亏 + 7日收益曲线合并为一个开关）/ 指数快照 / 涨跌与板块资金（涨跌家数 + 资金流向图 + 板块资金流合并为一个开关）/ 我的基金 / 我的股票。总览或「涨跌与板块资金」勾掉时对应整行收起，其余各块独立显隐。选择本地记忆，下次打开仍生效（默认全显示）。
- **移除两卡的「收起 / 展开」按钮**：「今日涨跌家数」「板块资金流」标题行右侧的收起按钮取消，两卡常显；历史折叠状态在启动时自动清除。
- **卡片显示扩充与联动**：「卡片显示」新增「我的基金」「我的股票」两个开关（勾掉即隐藏整个持仓明细区，含标题与表格）；「指数快照」开关与「指数显示设置」联动——开关打开但一个指数都没选时整块自动隐藏（含标题行），并在设置里的「指数快照」旁提示「未选任何指数，暂不显示」，在指数设置里增删指数立即生效。
- **指数快照改为二级菜单**：「卡片显示」里的「指数快照」勾选时立即弹出「指数显示设置」子面板，强制用户至少选择要展示的指数内容；取消勾选时不弹窗，并自动把指数选择置为「全不选」（卡片随之整块隐藏）。重新勾选时若当前为全不选，自动回填默认 7 个（沪市主板 / 沪深300 / 创业板指 / 科创50 / 中证500 / 恒生科技 / 国际金价），可在面板里继续调整。两个弹窗层叠正确（指数子面板位于设置面板之上），关闭后回到设置面板。
- **设置弹窗排版优化**：「指数卡片显示」入口在未勾选「指数快照」时置灰不可点（提示「需先勾选上方『指数快照』」）；汇率折算改为两列等宽、单位「元」紧跟输入框、实时汇率提示独占一行；「涨跌提醒」的全局阈值行与其它分区统一为卡片底；卡片显示与数据管理改为网格排列、选项不再被拉宽错位；「保存设置」从分区中间移到底部操作栏，与「关闭」同排（左「指数卡片显示」/ 右「保存设置 · 关闭」）；窄屏下汇率与卡片显示自动转单列。
- **7 日累计收益率卡底部排版优化**：「今日 / 近 7 日累计 / 跑赢」三行不再左右拉满，改为标签右对齐、数值与注释左对齐的两列网格，整块靠右聚拢——三行数值起始位置对齐，注释小字（「= 顶部今日盈亏卡」「(4基 45% · 2股 55%)」）紧跟数值，读数更集中。

> 发布状态：已发布（GitHub `main` = `19e186e`、tag `v1.1.4`；官网已上线；Edge 加载项商店 2026-09-22 已上架 v1.1.4）。

## v1.1.3（2026-09-21）
### 修复
- **汇率兜底值过期导致显示错误汇率**：离线兜底常数原为 `USD 7.1 / HKD 0.91`（更早的量级），而 2026-09-21 实际约 `USD 6.69 / HKD 0.853`，偏高约 6%；在「本地无汇率缓存 + 联网失败」时会被当成真实汇率显示。已按当日实际汇率校准，并把这一档在界面明确标为「离线估值」。
- **汇率缓存永不过期**：本地缓存此前无论多旧都标「实时」。现在 24h 内算「实时」，超过 24h 标注「缓存 N 小时前」，超过 7 天不再采信（退回兜底并在界面标注）。
- **无外币持仓时不请求汇率**：此前只有持有港美股才会拉取汇率，纯 A 股 / 基金用户打开「提醒设置 → 汇率折算」看到的实时提示长期是兜底常数。现在无外币持仓时，只要本地无缓存或缓存超 24h 也会补拉一次。
- **实时汇率取数通道失效（长期显示 7.1 / 兜底值的真因）**：此前实时汇率取的是东财 `push2.eastmoney.com/api/qt/ulist.np/get`，该接口已被服务端掐断（2026-09-21 实测：同一主机 `/api/qt/clist/get` 正常返回 200，而 `ulist.np/get`、`stock/get` 一律空响应 / `server closed abruptly`），扩展因此永远拿不到实时值、只能长期显示兜底常数——**仅校准常数并不能修复这一点**。现改为腾讯外汇行情 `qt.gtimg.cn/q=fxUSDCNY,fxHKDCNY`（与股票行情同一条已验证可用的通道，返回 GBK 文本，按 `~` 切分取最新价），东财降为备用源；两路都失败才用本地缓存 / 兜底常数。

> 发布状态：已发布（GitHub `main` = `6d3a013`、tag `v1.1.3` = `a22378e`；官网已上线；Edge 加载项商店 2026-09-22 过审上架）。

## v1.1.2（2026-09-21）
### 新增
- **隐私打码**：持仓市值卡标题行新增「眼睛」按钮，一键把 10 处金额（总市值 / 今日盈亏 / 累计盈亏 / 已实现 / 基金·股票盈亏等）遮为圆点并灰化，累计小结与迷你柱状图同步模糊；开关状态本地记忆，下次打开仍是打码态，且加载 / 刷新期间不出现明文闪现。
- **港美股汇率折算**：美股 / 港股按实时汇率（离岸人民币 USDCNH / HKDCNH）折算为人民币后计入总市值、今日盈亏、累计盈亏与已实现，修正此前「美元按 1:1 当人民币入账」导致总市值被系统性放大的口径错误；「提醒设置」新增「汇率折算」手填覆盖（留空 = 自动获取）。

### 优化
- 外币持仓行内「市值 / 今日盈亏 / 累计盈亏 / 成本价 / 已实现 / 现价」六列统一为「人民币折算值为主 + 原币小字」（如 `546.46` / `HK$600.50`；现价沿用 2 位、成本价沿用 3 位小数），与同一行其余列完全同基准、可直接比高低；A 股与场内基金显示不变。
- 修正组合收益曲线中港美股持仓权重未折算的偏差：此前按「股数 × 现价」加权而未乘汇率，美元市值被按 1:1 计入，外币持仓在曲线里的权重被低估、曲线失真。
- 添加 / 编辑港美股时明确成本价与成交价的币种口径：区块标题显示「成本（港元 / 美元）」，字段标签显示「成本价（港元）」，占位提示给出对应量级的示例（如 600.500 / 200.000），并附一行「按港元填写（HK$），看板会按实时汇率折算成人民币计入总市值」，避免被误当人民币填写。

> 发布状态：已发布（GitHub main + tag v1.1.2、官网 beiyucloud.taoxinyuan.com 已上线；Edge 加载项商店已上线 v1.1.2）。

## v1.1.1（2026-09-10）
### 优化
- 板块资金流卡底部滑块改为半透明，不再遮挡竖排板块名；长板块名（如 PEEK材料）零遮挡。
- 竖排板块名字距更紧凑；含字母 / 数字 / 括号的板块名（如 Web3.0、PEEK材料(概念)）改为横排混排并缩窄占位，卡片整体更紧凑。
- 板块资金流卡与左侧「涨跌家数」卡严格等高：新增自动配平（收起 / 展开互不影响），全收起时两卡同高。
- 添加持仓弹窗优化：空状态提供示例芯片一键填入（161725 / 510300 / 00700.HK / AAPL）；输入框加搜索图标；识别出品种后才启用「确定」按钮；支持 Enter 确认 / Esc 关闭；识别成功后移除冗余的「基金名称」手填字段。
- 卡片标题「板块资金流（东方财富）」简化为「板块资金流」。
- 扩展名称改为「基金股票持仓看板」（商店展示名随发版同步；数据 / 功能不变）。

> 发布状态：待推送（GitHub / 官网 / Edge）。

## v1.1.0（2026-09-08）
### 新增
- 支持加仓 / 减仓：移动加权平均自动重算持仓均价，减仓自动记录已实现盈亏（落袋收益）；基金与股票均支持；顶部「累计盈亏」卡片新增「已实现」，表格新增「已实现(元)」列。
- 基金加仓 / 减仓按「日期 + 金额 / 份额」录入：选定日期后自动取该交易日收盘净值（净值只读展示，无需手填价格）——加仓按金额申购（自动算申购费与所得份额），减仓按份额赎回。
- 股票加仓 / 减仓按「份额 + 价格」录入，按设置里的交易成本（佣金费率 / 起收值 / 卖出印花税）实时估算手续费并预填，可手动改（免5账户起收值填 0）。
- 基金手续费按该基金真实费率自动算：申购取天天基金优惠费率（按金额自动选档，缓存 7 天）；赎回按「先进先出」逐笔算持有天数并套用对应赎回费率。取不到时用设置里的默认费率兜底（默认万 10，即一折 0.1%）。
- 新增「板块资金流」卡片（东方财富同源数据）：行业（128 个）/ 概念 / 地域三类板块切换，今日 / 5 日 / 10 日排行，柱状图对齐东财样式（流入红柱顶标数值、流出绿柱底标数值，底部滑块左右查看全部板块）。
- 涨跌家数卡新增市场情绪行：涨停 / 跌停家数（东财涨停池实时）+ 情绪温度徽章（过热 / 偏暖 / 中性 / 偏冷 / 冰点，按上涨占比自动分档）。
- 涨跌家数卡下半部新增大盘实时资金流向图：主力 / 超大单 / 大单 / 中单 / 小单五条分时累计净流入线（沪深两市合计），盘中实时更新。

### 优化
- 涨跌家数统计口径剔除 B 股，只统计沪深京 A 股全市场。
- 编辑 / 批量编辑保存时保留已记录的已实现盈亏，不丢失。
- 启动时自动为老用户的提醒设置补全新增字段默认值（`fundRateW` 等），避免升级后基金默认费率变成 0。
- 基金弹窗增加费率获取状态提示（获取中 / 天天基金优惠价 / 失败使用默认费率），大额固定费用档显示「每笔 N 元」。

> 发布状态：本地已就绪，待推送（GitHub / 官网 / Edge）。

## v1.0.9（2026-09-04）
### 新增
- 支持添加北交所持仓：识别 920 开头的北交所代码（或 bj 前缀）；北交所 2025-10-09 起全部切换为 920 号段，旧 8xx/43xx 历史持仓自动迁移到 920（后三位保留）。
- 指数显示设置弹窗新增「全不选」按钮：一键清空已选指数，首页指数卡片行自动折叠。

### 优化
- 初始安装清空默认示例持仓，改为「空仓」状态；空仓时页面引导点击顶部「+」添加持仓。
- 顶栏「批量编辑」按钮图标改为铅笔「编辑」符号。

> 发布状态：已随 **v1.0.9** 发布——GitHub `main` + tag `v1.0.9` + Release（含 zip）；官网 beiyucloud.taoxinyuan.com 同步上线；Edge 加载项商店已提交审核（v1.0.9 由用户于 2026-09-04 提交）。

## v1.0.8（2026-08-30）
### 新增
- 支持添加港股与美股；指数卡片支持自选（含港股、美股指数）；涨跌家数 / 领涨 / 领跌卡片支持收起与展示。

> 发布状态：已随 **v1.0.8** 发布——GitHub `main` + tag `v1.0.8` + Release（含 zip）；官网 beiyucloud.taoxinyuan.com 同步上线；Edge 加载项商店待提交审核（受 v1.0.6 / v1.0.7 审核队列阻塞）。

## v1.0.7（2026-08-26）
### 修复
- **基金公告提醒精准打开修复**：基金品种的公告提醒之前借用了股票公告的全文搜索接口。
- **公告弹窗「打开原文」按钮改用 PDF 原件链接**：修复
### 优化（代码质量）
- **修复图表内存泄漏**
- **移除重复的 ECharts 引入**
- **隐私页 CSS 变量对齐**
- **持仓刷新并行化**
- **公告弹窗新增「速读卡片」**
- **修复速读卡片在董事会决议公告上漏渲染 + 抓错议案**
- **速读卡片新增 4 类公告支持**

## v1.0.6（2026-08-26）
### 优化 / 修复（累计 v47–v52）
- **「我的股票」表列序重排**：「市值(元)」挪到「现价」之后、「涨跌幅」挪到「成本价」之后（基础→仓位→涨跌→收益归类更整齐）；合计行市值同步挪位，合计涨跌幅栏留空（加总无意义）。
- **批量编辑保存丢失「类型」字段修复**：`saveStockBatch` 漏写 `type`，凡走过一次批量保存该股票 type 被擦成 undefined → 渲染兜底成「其他」。修复：`saveStockBatch` 补 `type`（`prevTypeMap` 沿用原 type 或 `classifyStockType` 重分类）；`renderStocks` 渲染兜底 `s.type || classifyStockType(s.code).cls`（只显示不写回），存量「全部其他」立即恢复。
- **公告类提醒可点开查看详情**：提醒记录公告行可点，尾部「查看详情 ›」蓝色小尾巴；点击在扩展内弹窗拉取东财公告正文（走 SW 中转带 referer，宽容解析字段），失败给「重试」+「打开原文 ↗」双重兜底（原文链接恒可点）。价格类（涨跌幅）提醒不受影响。
- **修复 v49 公告弹窗「点不了」**：v49 `renderAlertLog` 误调不存在的 `escAttr()`，渲染到第一条公告即抛 `ReferenceError` 中断整段渲染。改为 `escHtml`（本身已转义引号）+ 分支加固：新结构化公告→内联弹窗，旧纯文本公告→按标题跳东财搜索，价格条目保持原样。
- **浅色主题识别卡片字体对比提升**：添加持仓弹窗识别卡（`ident-card`/`seg.active`）浅色下原用透明蓝渐变 + 白字反差差；改为浅色专用实色深蓝渐变（`#2f6fe0→#1849b8`）+ 白字 + 阴影，与未选中卡（面板底 + 深色字）层次分明；深色保持原观感。
- **隐私政策页跟随浅 / 深主题**：`privacy.html` 变量化并新增 `[data-theme="light"]` 覆盖；修 MV3 CSP 兼容——原内联 `<script>` 被 `script-src 'self'` 屏蔽（导致「没变化」），改为外部 `privacy.js`（`'self'` 合法），读取与看板同源共享的 `fund_board_theme` 并监听 `storage` 事件实时跟随。
- **语义版本号 1.0.5 → 1.0.6**（`manifest.json`）。

> 发布状态：随 v1.0.6 发布流程同步（GitHub main + tag v1.0.6 + Release 含 zip；官网 beiyucloud.taoxinyuan.com 上线 v1.0.6；Edge 加载项商店提交审核）。

## v1.0.5（2026-08-25）
### 优化
- **提醒面板合并：顶部"仅停提醒"芯片并入单品种阈值行（基金/股票）**
  - 顶部"当前提醒监控"块去除，每只基金/股票行的最右侧直接放「关闭提醒」按钮 / 停后变「恢复提醒」（行变灰）。同一只股票不再在两处出现。
  - 基金也补齐"单只停提醒"能力（新增 `fundMuted`，与 `stockMuted` 平行；`defaultAlerts` 默认 `{}`，估算涨幅循环跳过已停项）。
  - 实现：`dashboard.html` 删监控摘要 div + 旧 `.monitor-scope`/`.stk-scope` 样式、新增行内 `.row-act`/`.is-muted`，按钮由圆形 × 改为小文字标签（"关闭提醒"/"恢复提醒"，同 4 字宽避免抖动），窄屏(≤768px)减小间距与输入框宽度以避免按钮被裁；`dashboard.js` 删 `renderMonitorScope`，新增 `renderAlertRows` + `muteFundAlert`/`unmuteFundAlert` + `toggleRowMute`（局部切行，不重渲整个列表，避免把未保存的阈值输入清掉）。
- **扩展内新增「隐私政策」入口**
  - 看板页脚新增「隐私政策」文字链接，点击在扩展内新标签页打开 `privacy.html`（本地文件、无需联网），与「数据全部存本地、不上云」的隐私主张一致。
  - 同步将页脚展示用「看板版本」从 v32 升到 v33（2026-08-25）。
- **CI：Edge 自动发布 workflow 修复**
  - `.github/workflows/release-edge.yml` 增加 `permissions: { contents: read, issues: write }`，避免「失败通知」step（actions/github-script 创建 issue）因默认 GITHUB_TOKEN 只读而报 `Resource not accessible by integration`。
  - 仓库 Secrets 补齐 `EDGE_API_KEY` / `EDGE_CLIENT_ID` / `EDGE_PRODUCT_ID`，以后推 `v*` tag 即可自动打包并向 Edge 商店提交审核。

> 发布状态：已推送 GitHub（main + tag v1.0.5 + Release 含 zip/crx）；官网 beiyucloud.taoxinyuan.com 同步上线 v1.0.5；Edge 加载项商店审核中。

## v1.0.4（2026-08-19）

### 修复 / 优化
- **桌面通知标题改为「持仓看板提醒」**
  - 修复：通知标题原本写死「基金看板提醒」，但看板早已支持股票 / ETF / LOF / 可转债 / REITs 等场内品种，标题局限在"基金"会误导用户以为只有基金在提醒。
  - 现在：`dashboard.js` 中 `pushNotify('基金看板提醒', msg)` 改为 `pushNotify('持仓看板提醒', msg)`（与测试授权通知标题「持仓看板通知」保持一致）。
- **Edge 商店描述补全「股票 / 场内基金实时行情」**
  - `manifest.json` description 由「本地持仓盈亏/基金估值/价格异动提醒看板」改为「本地持仓盈亏/基金估值/股票·场内基金(ETF·LOF·可转债·REITs)实时行情/价格异动提醒看板」，避免商店页只提基金、漏掉已支持的全品类。
- **全局阈值过低提示文案去「基金」本位**
  - `dashboard.js` 中"偏低，基金日常波动易触发噪音"改为"偏低，持仓日内波动易触发噪音"——股票日内波动更猛，单提基金不准确。
- **空持仓 UX 收口（卡片按需显示 + 今日盈亏占位）**
  - 修复：未添加任何基金 / 股票时，页面仍空跑两张「我的基金 / 我的股票」卡片（含"暂无持仓"提示行），既占空间又让今日盈亏卡出现空洞的 `0`。
  - 现在：
    - 「我的基金」section（`#fundSection`）= `holdings.length === 0` 时**整段（标题 + 表）一起隐藏**；
    - 「我的股票」section（`#stockSection`）= `stocks.length === 0` 时**整段隐藏**；
    - 「今日盈亏」汇总卡：基金 + 股票都未持仓时，主数字显示灰色「**未持仓**」、基金/股票拆分行也落「未持仓」、累计盈亏 chip 与市值副框复位为占位（录入提示 / `--`），不再显示空洞的 `0`。
  - 只要任一品种有持仓，对应 section 与今日盈亏卡按原逻辑正常显示。
- **「今日盈亏」拆分两行严格对称（基金/股票各自"未持仓"占位）**（补丁）
  - 修复：上一条空持仓 UX 收口只处理了"两都为空"和"无股票有基金"两种场景。当「**有股票无基金**」时，基金那一行仍回退到 `fmtSigned(0) = "0"`，与"有基金无股票"时的"未持仓"提示不对称。
  - 根因：`updateSummary` 里 `pnlFund / pnlStock` 用一个 `if(sRes.count > 0){...} else {...}` 同时设两行，"有股票"分支走 `fmtSigned(fSplit, 0)`，所以 `fSplit=0` 时基金那行显示 `0`。
  - 现在：拆成两个独立 `if/else`，分别按 `fRes.fundCount > 0` / `sRes.count > 0` 判定——无持仓品种统一显示灰色「未持仓」（`className='val muted'`），有持仓品种显示数值与红绿。
- **批量编辑面板「删除」按钮无功能修复**（补丁）
  - 修复：用户点击「批量编辑持仓」里的「删除」链接没有任何反应，被误判为无功能。
  - 根因：HTML 上挂了 `data-act="rowDel"`，但全文件从未定义 `window.rowDel` 函数——MV3 单根委托找不到处理函数就静默丢弃（`dashboard.js:9` `bindMv3Delegation`）。
  - 现在：补上 `rowDel(e)` 函数（`dashboard.js:1244-1252`）从 DOM 移除当前 `<tr>`，保存时按剩余 tr 收集，被删行自然不写入新持仓；若删到一格不剩自动补一行空行。同时给两个删除链接加 `data-args='["event"]'` 让事件透传；`.row-del` 加 `:hover { text-decoration: line-through; opacity: .75; }` 视觉反馈。
- **新增「官网」入口（beiyucloud.taoxinyuan.com）**
  - 看板 UI 新增指向落地页（使用说明 / 下载 / 更新日志 / 常见问题）的入口，方便用户从扩展内直接访问官方站点。
  - 顶栏：在「☕ 打赏」按钮前新增「官网」按钮，点击新标签页打开 `https://beiyucloud.taoxinyuan.com`（`<a class="btn" target="_blank" rel="noopener noreferrer">` 直跳，无需 JS、不受扩展 CSP `connect-src` 限制）。
  - 底部 footer：版权 / 数据通道行追加「持仓实时盈亏看板」文字链接（同域名新标签页打开）。
  - `manifest.json` 新增 `homepage_url` = `https://beiyucloud.taoxinyuan.com`，商店详情页展示「主页」链接。

## v1.0.3（2026-08-17）

### 修复 / 优化
- **顶部状态标签改用「真实数据」判定**
  - 修复：收盘后即便当日净值已公布，顶部仍一直显示「收盘后·显示收盘估值（待净值公布）」。
  - 现在：当 `marketStatus()` 检测到本次拉到的基金里已有 `estType = nav`（即今日 NAV 已公布），自动把顶部标签切到「**净值已公布 · MM-DD HH:MM**」，避免「永远待」的误读。
  - 判定窗口：仅覆盖盘后时段（`afterClose=true`），不影响盘中「实时估值」与盘前/周末「昨日净值」的标签。
- **「净值已公布」日期格式修正（补丁）**
  - 修复：之前用 `(\d{2})-(\d{2})` 在 `gztime="2026-08-26 已更新"` 上从最左匹配，会撞到「2026-08」里的"26-08"（年份前两位被当成月份），结果显示成 `净值已公布 · 26-08`。
  - 现在：日期直接取权威源 `info.navDate.slice(5, 10)` 切 MM-DD（"2026-08-26" → "08-26"），`gztime` 只抓 `HH:MM`，输出 `08-26 14:30` 或 `08-26 已公布`。
- **角标「1 位小数四舍五入」格式（bg.js）**
  - 之前：1000~9999 整数 k（1189 → "1k"）、10000+ 整数 w，损失精度。
  - 现在：1000~9999 → 1 位小数 k（1189 → "1.2k"）；10000~99999 → 1 位小数 w（12345 → "1.2w"）；100000+ → 整数 w（123456 → "12w"）兜底，避免 4 字符硬限溢出。
  - 不再带 `+`/`-` 符号——方向完全交给背景色红/绿（红涨绿跌，bg.js line 31 已实现）。带符号 1 位小数 k/w 是 5 字符，硬上限 4 字符带不动。
- **「清空持仓后刷新不再回填默认示例」（dashboard.js）**
  - 修复：用户把默认 3 只示例基金（161725 / 110022 / 003096）全删或全编辑后，刷新页面 / 重开看板时，3 只默认示例又会被重新塞回，用户的真实持仓被覆盖。
  - 根因：`loadHoldings()` 用 `Array.isArray(h) && h.length` 判"已初始化"，把「空数组」误判成「未初始化」，于是回退到内置种子并 `saveHoldings()` 写回 localStorage。
  - 现在：改成 `getItem(key) !== null` 才认作"已初始化"，空数组也会被尊重（用户主动清空 = 保持空）。仅在「从未打开过本扩展」时才会一次性看到默认 3 只作为引导。

## v1.0.2（2026-08-17）

### 修复 / 优化
- **持有收益率改用「已结算净值」口径**
  - 盘中、盘后（净值未公布）时段，持有收益率与累计盈亏不再随实时估算净值跳动，固定显示**上一交易日确认净值**；
  - 仅当当日新净值公布（`estType = nav`）后，才自动更新为最新值。
  - 一句话：估值不显示持有收益率这项，只显示已结算的。
- 今日盈亏、估算涨幅仍按实时估值显示（保留原有口径，便于盘中跟踪当日波动）。
- 同步更新看板底部「计算口径」说明文案，明确持有收益率的已结算口径。

### 影响范围
基金表「持有收益率」列、合计行「累计盈亏」、顶部卡「持有收益率」三处展示统一为已结算口径。

---

## v1.0.1（2026-08-11）
- 上架 Microsoft Edge 加载项商店（一键安装、无安全警告、自动更新）。
- 补充隐私说明页（privacy.html）。

## v1.0.0
- 首个发布版本：本地持仓盈亏 / 基金估值 / 价格异动提醒看板，数据全部存本地、不上云。
